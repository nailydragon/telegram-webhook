services:
  - type: web
    name: telegram-ai-webhook
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py  # This line tells Render which file to run
    autoDeploy: true
